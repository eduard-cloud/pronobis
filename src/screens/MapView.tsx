type Props = {
  onSelectPerson: (id: string) => void;
};

export function MapView(_props: Props) {
  return (
    <div style={{ padding: '0 20px 120px' }}>
      <p className="t-body">Map view placeholder.</p>
    </div>
  );
}
